{
  "name": "codebook",
  "version": "1.0.0",
  "description": "Dynamic generate password, encode/decode and transfer to a serve for use anywhere ",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "YY",
  "license": "ISC",
  "dependencies": {
    "generate-password": "^1.3.0",
    "inquirer": "^3.0.6",
    "yargs": "^7.1.0"
  }
}
